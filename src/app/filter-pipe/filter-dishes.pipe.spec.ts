import { FilterDishesPipe } from './filter-dishes.pipe';

describe('FilterDishesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDishesPipe();
    expect(pipe).toBeTruthy();
  });
});
